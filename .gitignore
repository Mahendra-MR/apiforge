node_modules/
dist/
build/
coverage/
.env
.env.local
*.log
.DS_Store
*.db
*.db-shm
*.db-wal

# electron-builder's packaged output, its conventional icon-source folder
# (which the blanket `build/` rule above would otherwise exclude), and the
# backend/frontend builds copied in by `copy-embed` before packaging.
apps/desktop/release/
apps/desktop/backend/
apps/desktop/frontend/
!apps/desktop/build/
!apps/desktop/build/**
